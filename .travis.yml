language: cpp

compiler:
  - gcc
  - clang

os:
  - linux
  - windows
  - osx

arch:
  - amd64
  - arm64
  - ppc64le
  - s390x

script:
  - cd build
  - if[! -f "CMakeCache.txt"]; then rm CMakeCache.txt fi
  - cmake ../
  - cmake --build .
